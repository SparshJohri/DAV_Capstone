module assignmentOperator #(parameter WIDTH = 32)
(
	output [WIDTH-1:0] recipient
	output [WIDTH-1:0] original
);

	genvar i;
	for (i=0; i<WIDTH; i++)
	begin
		recipient[i] = original[i];
	end

endmodule